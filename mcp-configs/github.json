{
  "name": "github",
  "description": "GitHub API integration - PR, Issue, Repository management (API key required: GITHUB_PERSONAL_ACCESS_TOKEN)",
  "requiresApiKey": true,
  "apiKeyEnvVar": "GITHUB_PERSONAL_ACCESS_TOKEN",
  "config": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
    }
  }
}
