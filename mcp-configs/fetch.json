{
  "name": "fetch",
  "description": "Fetch URL content - HTML/JSON/Markdown conversion (free, no API key required)",
  "requiresApiKey": false,
  "config": {
    "command": "uvx",
    "args": ["mcp-server-fetch"]
  }
}
